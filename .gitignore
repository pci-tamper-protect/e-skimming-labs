# Node modules
node_modules/
package-lock.json
!test/package-lock.json

# npm cache files
**/_update-notifier-last-checked
**/clean/

deploy/traefik/debug/*
.env.keys.*
.env.keys
.env.*.bak.*
.env.*.bak

.playwright-mcp/traces/

# Stolen data
**/stolen-data/*
!*/stolen-data/.gitkeep
labs/01-basic-magecart/test/test-results/
labs/01-basic-magecart/malicious-code/c2-server/stolen-data/master-log.jsonl
labs/04-steganography-favicon/c2-data/master-log.jsonl
**/c2-data/*
*.jsonl
.last-run.json

**/error-context.md

**/playwright-report/*
**/test-results/*
test/.auth/

*.png
*.webm

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
# Note: Encrypted .env.stg and .env.prd files ARE tracked (they're encrypted, safe to commit)
# BUT: deploy/.env.* files should NEVER be committed (only root-level .env.* files exist)
deploy/.env.*
deploy/.env
# Private keys (.env.keys.*) are NOT tracked (must be kept secret)
# Hash files (.env.hashes.*) ARE tracked (for audit trail)
# dotenvx-ops project files (.env.x, .env.x.*) ARE tracked (contains only public DOTENVX_PROJECT_ID)

# Terraform
**/.terraform/**
**/*.tfstate
**/*.tfstate.*
**/tfplan
**/*.tfvars
**/*.tfvars.json
private-data/

# Service Account Keys (should not be committed)
deploy/*-sa-key.json
deploy/*.json
*.json
!package.json
!package-lock.json
!test/package-lock.json
!go.mod
!go.sum
!tsconfig.json

test/MITRE_ATTACK_FIX_REPORT.md

# Provider plugin vendor directory (not needed for sidecar builds)
deploy/traefik/plugins-local/**/vendor/

# Temporary GitHub clone of traefik-cloudrun-provider (CI/CD fallback)
deploy/traefik/.traefik-cloudrun-provider-clone/

# dotenvx encryption keys (added by dotenvx-converter)
.env.*.keys

# dotenvx encryption keys (added by dotenvx-converter)
.env.prd.keys
.env.prod.keys
.env.production.keys
