#!/bin/bash
# Alias helper - add this to your shell profile to use 'dc' instead of 'docker-compose'
# Usage: source .docker-compose-alias.sh
# Then: dc up, dc down, etc.

alias dc='./docker-compose.local.sh'
