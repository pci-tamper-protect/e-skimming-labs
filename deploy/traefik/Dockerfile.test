# Traefik Dockerfile for local testing
# Mirrors Dockerfile.cloudrun but optimized for local development

FROM traefik:v3.0

# Install curl and bash for health checks and entrypoint
USER root
RUN apk add --no-cache curl ca-certificates bash

# Copy Traefik configuration
COPY traefik.cloudrun.yml /etc/traefik/traefik.yml
COPY dynamic/ /etc/traefik/dynamic/

# Copy entrypoint script that generates dynamic config from env vars
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh && \
    ls -la /entrypoint.sh && \
    test -f /entrypoint.sh && \
    test -x /entrypoint.sh && \
    echo "✅ entrypoint.sh copied and made executable"

# Note: Running as root for local testing (Cloud Run version switches to traefik user)

# Expose ports
EXPOSE 80 443 8080

# Health check
HEALTHCHECK --interval=10s --timeout=3s --retries=3 \
    CMD traefik healthcheck --ping || exit 1

# Use custom entrypoint
ENTRYPOINT ["/entrypoint.sh"]
CMD ["traefik"]
