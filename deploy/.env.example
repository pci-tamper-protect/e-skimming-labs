# E-Skimming Labs Environment Configuration
# Copy this file to .env.prd or .env.stg and update with your values
# Then create a symlink: ln -s .env.prd .env

# Labs Project Configuration
LABS_PROJECT_ID=labs-prd
LABS_REGION=us-central1

# Home Project Configuration (for deploy-home.sh and deploy-labs.sh)
HOME_PROJECT_ID=labs-home-prd
HOME_REGION=us-central1
